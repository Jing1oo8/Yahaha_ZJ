# SQLite 数据库地址；默认文件由 seed 命令创建，禁止提交数据库文件
DATABASE_URL=sqlite:///data/app.db

# 最终 ItemCF gzip 模型；通过 final 训练命令生成，禁止提交模型文件
MODEL_PATH=models/itemcf-0022f60b5e4b.json.gz

# 时间切分后生成的拟合期用户历史；用于线上个性化初始化
USER_HISTORY_PATH=data/processed/user_history.jsonl
